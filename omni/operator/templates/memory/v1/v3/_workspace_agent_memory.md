```yaml
memory/ (layout v3)

Agent directories are located directly under memory/:
  memory/<agent_name>/

To navigate available agents:
  ls memory/

To find an agent's instructions:
  memory/<agent_name>/instructions/memory.md

Collab tasks between agents:
  memory/collab/tasks/<agent_name>/

Workspace default version policy:
  memory/version.yaml
```
